{
  "name": "proton-cloudflare-wkd",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.1",
    "@types/node": "^25.6.0",
    "tslint": "^6.1.3",
    "typescript": "^6.0.3",
    "vitest": "~4.1.5",
    "wrangler": "^4.86.0"
  }
}
